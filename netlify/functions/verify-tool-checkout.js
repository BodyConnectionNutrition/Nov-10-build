const crypto=require("crypto");
const PRODUCTS={
 "who-taught-you-to-eat":{priceEnv:"STRIPE_PRICE_WHO_TAUGHT_YOU_TO_EAT"},
 "how-was-my-body-image-created":{priceEnv:"STRIPE_PRICE_BODY_IMAGE"},
 "deconstructing-a-belief":{priceEnv:"STRIPE_PRICE_DECONSTRUCTING_BELIEF"},
 "behavior-sequence":{amount:2900},"permission-and-scarcity":{amount:2900},"what-is-this-doing-for-me":{amount:2900},"choice-has-conditions":{amount:2900},"my-food-and-body-framework":{amount:2900},"values-clarification":{priceId:"price_1U7NfFPfe7PZZ7IATKAFHLtm"}
};
async function stripeGet(path){const key=process.env.STRIPE_SECRET_KEY;if(!key)throw new Error("Stripe is not configured");const response=await fetch(`https://api.stripe.com${path}`,{headers:{Authorization:`Bearer ${key}`}});if(!response.ok)throw new Error(`Stripe request failed: ${response.status}`);return response.json();}
function sign(payload){const secret=process.env.ACCESS_TOKEN_SECRET;if(!secret)throw new Error("Access token signing is not configured");const encoded=Buffer.from(JSON.stringify(payload)).toString("base64url");const signature=crypto.createHmac("sha256",secret).update(encoded).digest("base64url");return`${encoded}.${signature}`;}
exports.handler=async(event)=>{const qs=event.queryStringParameters||{};const sessionId=qs.session_id||"",product=qs.product||"",config=PRODUCTS[product];if(!config||!sessionId.startsWith("cs_"))return{statusCode:400,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify({ok:false,error:"Missing or invalid checkout information."})};try{const session=await stripeGet(`/v1/checkout/sessions/${encodeURIComponent(sessionId)}`);const items=await stripeGet(`/v1/checkout/sessions/${encodeURIComponent(sessionId)}/line_items?limit=10`);let hasProduct=false;if(config.priceId){hasProduct=Array.isArray(items.data)&&items.data.some(item=>item.price&&item.price.id===config.priceId);}else if(config.priceEnv){const priceId=process.env[config.priceEnv];if(!priceId)throw new Error("Verification price is not configured");hasProduct=Array.isArray(items.data)&&items.data.some(item=>item.price&&item.price.id===priceId);}else{hasProduct=session.amount_total===config.amount&&session.currency==="usd"&&Array.isArray(items.data)&&items.data.some(item=>item.amount_total===config.amount&&item.price&&item.price.currency==="usd");}const metadataMatches=session.metadata&&session.metadata.product_slug===product;if(session.status!=="complete"||session.payment_status!=="paid"||!hasProduct||!metadataMatches)return{statusCode:403,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify({ok:false,error:"Purchase could not be verified."})};const email=(session.customer_details&&session.customer_details.email)||session.customer_email||"";const token=sign({sid:session.id,product,email,v:1});return{statusCode:200,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify({ok:true,token,product,email})};}catch(error){console.error(error);return{statusCode:500,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify({ok:false,error:"Verification is temporarily unavailable."})};}};
